<form method="post" action="{{ path('simple_delete', {'id_user': simple.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ simple.idUser) }}">
    <button class="btn"><i class="ti-trash"></i></button>
</form>
