{% extends 'backend2/page.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{asset("backend/chedli/acceuil.css")}}">
{% endblock %}
{% block title %}Tache{% endblock %}

{% block param %}
<div id="dynamicbull">
    <h1>Tache</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTache</th>
                <td>{{ tache.idTache }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ tache.username }}</td>
            </tr>
            <tr>
                <th>EtatTache</th>
                <td>{{ tache.etatTache ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>DifficulteTache</th>
                <td>{{ tache.difficulteTache }}</td>
            </tr>
            <tr>
                <th>DescriptionTache</th>
                <td>{{ tache.descriptionTache }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tache_index') }}">back to list</a>

    <a href="{{ path('tache_edit', {'idTache': tache.idTache}) }}">edit</a>

    {{ include('tache/_delete_form.html.twig') }}
</div>
{% endblock %}
