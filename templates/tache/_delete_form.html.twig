<form method="post" action="{{ path('tache_delete', {'idTache': tache.idTache}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tache.idTache) }}">
    <button class="btn">Delete</button>
</form>
