{% extends 'baseAdmin.html.twig' %}

{% block title %}Nutritionist{% endblock %}

{% block body %}
    <h1>Nutritionist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Username</th>
                <td>{{ nutri.username }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ nutri.mail }}</td>
            </tr>
            <tr>
                <th>Date_n</th>
                <td>{{ nutri.dateN ? nutri.dateN|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <button class="btn" onclick="location.href='{{ path('nutri_index') }}'"> <i class="fa fa-reply"></i> </button>
<div class="col-offset-5">
    <button class="btn" onclick="location.href='{{ path('nutri_edit', {'id': nutri.id}) }}'" > <i class="fa fa-edit"> </i></button>
    {{ include('nutri/_delete_form.html.twig') }}
{% endblock %}
