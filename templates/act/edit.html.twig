{% extends 'Front.html.twig' %}

{% block title %}Edit Act{% endblock %}

{% block Evenement %}
    <h1>Edit Act</h1>

    {{ include('act/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a class="btn btn-outline-primary fas fa-reply" href="{{ path('act_index') }}">Revenir à la liste</a>

    {{ include('act/_delete_form.html.twig') }}
{% endblock %}
