<!DOCTYPE html>

<title>Invitation</title>

{% block body %}
    <h1>Invitation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invitation.id }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ invitation.idUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('invitation_index') }}">back to list</a>

    <a href="{{ path('invitation_edit', {'id': invitation.id}) }}">edit</a>

    {{ include('invitation/_delete_form.html.twig') }}
{% endblock %}
