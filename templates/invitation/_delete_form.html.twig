<form method="post" action="{{ path('invitation_delete', {'id': invitation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invitation.id) }}">
    <button class="btn btn-outline-danger" ><i class="fa fa-trash " ></i> Supprimer</button>
</form>
