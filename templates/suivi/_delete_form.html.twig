<form method="post" action="{{ path('suivi_delete', {'idS': suivi.idS}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi.idS) }}">
    <button class="btn">Delete</button>
</form>
