<form method="post" action="{{ path('psycho_delete', {'idUser': psycho.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ psycho.idUser) }}">
    <button class="btn">Delete</button>
</form>
