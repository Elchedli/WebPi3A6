{% extends 'base.html.twig' %}

{% block title %}Psycho{% endblock %}

{% block body %}
    <h1>Psycho</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdUser</th>
                <td>{{ psycho.idUser }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ psycho.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ psycho.password }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ psycho.mail }}</td>
            </tr>
            <tr>
                <th>DateN</th>
                <td>{{ psycho.dateN ? psycho.dateN|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ psycho.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('psycho_index') }}">back to list</a>

    <a href="{{ path('psycho_edit', {'idUser': psycho.idUser}) }}">edit</a>

    {{ include('psycho/_delete_form.html.twig') }}
{% endblock %}
