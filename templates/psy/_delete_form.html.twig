<form method="post" action="{{ path('psy_delete', {'id': psy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ psy.id) }}">
    <button class="btn"><i class="ti-trash"></i></button>
</form>
