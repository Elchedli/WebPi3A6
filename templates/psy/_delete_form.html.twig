<form method="post" action="{{ path('Psycho_delete', {'id': Psycho.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ Psycho.id) }}">
    <button class="btn"><i class="ti-trash"></i></button>
</form>
