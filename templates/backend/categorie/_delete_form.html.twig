<center>
<form method="post" action="{{ path('categorie_deletee', {'idCat': categorie.idCat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.idCat) }}">
    <button class="btn btn-danger">Delete</button>
</form>
</center>
