<form method="post" action="{{ path('Psychofront_delete', {'id_user': Psycho.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ Psycho.id) }}">
    <button class="btn custom-btn">delete</button>
</form>
