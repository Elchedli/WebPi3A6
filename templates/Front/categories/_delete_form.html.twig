<form method="post" action="{{ path('categories_delete', {'idCat': category.idCat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.idCat) }}">
    <button class="btn">Delete</button>
</form>
