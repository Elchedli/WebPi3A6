<center>
<form method="post" action="{{ path('articles_delete', {'idArt': article.idArt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.idArt) }}">
    <button class="btn custom-btn">Delete</button>
</form>
</center>
