<form method="post" action="{{ path('categories_delete', {'idCat': categories.idCat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories.idCat) }}">
    <button class="btn btn-adn">Delete</button>
</form>
