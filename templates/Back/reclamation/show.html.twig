{% extends 'baseback.html.twig' %}
{% block title %}
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Spirity | Reclamations</title>
{% endblock %}
{% block b %}
    <h1>Categories</h1>
<div class="box box-info" style="align-items: center; padding-left: 400px ">
    <div class="box-header">
        {# <i class="fa fa-envelope"></i> #}
<h3 class="box-title">Reclamation</h3>
</div>
<div class="box-body">
    <form action="#" method="post">
        <div class="form-group">
            <table class="table">
                <thead>
                <tbody>
        {#   <tr>
               <th>IdRec</th>
                <td>{{ reclamation.idRec }}</td>
            </tr> #}
            <tr>
                <th>Username</th>
                <td>{{ reclamation.username }}</td>
            </tr>
            <tr>
                <th>Object</th>
                <td>{{ reclamation.objRec }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ reclamation.sujRec }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reclamation.etatRec }}</td>
            </tr>
        <tr>
            <th>Category</th>
            <td>{{ reclamation.idCat }}</td>
        </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.dateRec ? reclamation.dateRec|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-adn" href="{{ path('reclamation_indexx') }}">Back To List</a>
    {{ include('Back/reclamation/_delete_form.html.twig') }}
    <a class="btn btn-adn" href="{{ path('reclamation_editt', {'idRec': reclamation.idRec}) }}">Edit</a>
        </div>
    </form>
</div>
</div>
{% endblock %}
