<center>
    <form method="post" action="{{ path('reclamation_deletee', {'idRec': reclamation.idRec}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.idRec) }}">
        <button class="btn btn-soundcloud"> <i class="fa fa-trash"></i> Delete</button>
    </form>
</center>
