{% extends 'backend2/page.html.twig' %}

{% block title %}
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Spirity | Categories</title>
{% endblock %}

{% block param %}

    <div class="box box-info" style="align-items: center; padding-left: 400px ">
    <div class="box-header">
        <table class="table">
            <tbody>
            <!-- <tr>
                <th>Id_ph</th>
                <td>{{ photo_publication.idPh }}</td>
            </tr>
            <tr>
                <th>Id_pub</th>
                <td>{{ photo_publication.idPub }}</td>
            </tr>-->
            <tr>
                <th>Photo</th>
                <td><img src="{{ photo_publication.lien | raw }}" alt="Trulli" width="500" height="333"></td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('photos_publication',{'id_pub': photo_publication.idPub}) }}">Return</a>

        <a href="{{ path('photo_publication_edit', {'id_ph': photo_publication.id_ph}) }}">edit</a>

        {{ include('photo_publication/_delete_form.html.twig') }}

    </div>
    </div>
{% endblock %}