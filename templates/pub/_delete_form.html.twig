<form method="post" action="{{ path('pub_delete', {'id_pub': publication.getid_pub()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publication.id_pub) }}">
    <button class="btn">Delete</button>
</form>
