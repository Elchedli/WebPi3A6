{% extends 'Front.html.twig' %}

{% block title %}Edit Evenement{% endblock %}

{% block Evenement %}
    <center>
    <h1>Edit Evenement</h1>

    {{ include('evenement/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <br>
    <br>
    <a class="btn btn-outline-primary fas fa-reply"  href="{{ path('evenement_index') }}">Revenir à la liste</a>
    <br>
    <br>
    {{ include('evenement/_delete_form.html.twig') }}
    </center>
{% endblock %}
