{% extends 'baseback.html.twig' %}
{% block title %}
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Spirity | Categories</title>
{% endblock %}

{% block b %}
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}})}}
        <div>
            <div class="pt-editor">
                <div class="form-group col-md-12">
                    <div class="box-header">

                        <center> <h3 class="box-title" style="font-size: 40px;color:#1d5987;" >Editer Catégories</h3></center>
                    </div>
                    <label for="inputContenu" class="tt-note pt-4">Catégorie :</label>
                    {{ form_errors(form.titreCat) }}
                    {{ form_widget(form.titreCat, { 'attr' : { 'class' : 'form-control', "rows":"5" } }) }}
                </div>
            </div>
<center>
    <button class="btn btn-success">{{ button_label|default('Update') }}</button>
            {{ form_end(form) }}


    <a href="{{ path('categorie_indexx') }}" class="btn btn-info">Back to list</a>
</center>
<br>
    {{ include('Backend/categorie/_delete_form.html.twig') }}
{% endblock %}
